apiVersion: v1
kind: Service
metadata:
  name: {{ include "sensor-quality-inference.fullname" . }}
  labels:
    {{- include "sensor-quality-inference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sensor-quality-inference.selectorLabels" . | nindent 4 }}
